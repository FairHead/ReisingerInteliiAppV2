<?xml version="1.0" encoding="utf-8" ?>
<ContentView
    x:Class="ReisingerIntelliApp_V4.Components.PlacedDeviceControl"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:converters="clr-namespace:ReisingerIntelliApp_V4.Converters"
    xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
    x:Name="placedDeviceControl"
    BackgroundColor="Transparent"
    Loaded="OnControlLoaded">

    <ContentView.Resources>
        <ResourceDictionary>
            <converters:BoolToOpenCloseConverter x:Key="BoolToOpenCloseConverter" />
        </ResourceDictionary>
    </ContentView.Resources>

    <!--  Main Container - AbsoluteLayout for overlay positioning (T032 fix)  -->
    <AbsoluteLayout
        x:Name="MainContainer"
        HeightRequest="400"
        HorizontalOptions="Center"
        VerticalOptions="Center"
        WidthRequest="400">

        <Grid
            AbsoluteLayout.LayoutBounds="0.5,0.165,200,50"
            AbsoluteLayout.LayoutFlags="PositionProportional"
            ColumnDefinitions="Auto,*,Auto"
            HeightRequest="50"
            VerticalOptions="Start">

            <!--  Links  -->
            <ImageButton
                x:Name="ScalePlusDeviceButton"
                Grid.Column="0"
                Padding="10"
                BackgroundColor="LightGrey"
                BorderColor="Grey"
                BorderWidth="4"
                Clicked="OnScalePlusClicked"
                CornerRadius="25"
                HeightRequest="50"
                Source="plus.svg"
                WidthRequest="50" />

            <!--  Spacer (mittlere Spalte)  -->
            <BoxView Grid.Column="1" BackgroundColor="Transparent" />

            <!--  Rechts  -->
            <ImageButton
                x:Name="ScaleMinusDeviceButton"
                Grid.Column="2"
                Padding="10"
                BackgroundColor="LightGrey"
                BorderColor="Grey"
                BorderWidth="4"
                Clicked="OnScaleMinusClicked"
                CornerRadius="25"
                HeightRequest="50"
                Source="minus.svg"
                WidthRequest="50" />

        </Grid>



        <!--  Move Buttons Container (T032: Overlay with HaloSpacing, always above Card)  -->
<AbsoluteLayout
    x:Name="ArrowButtonsContainer"
    AbsoluteLayout.LayoutBounds="0,0,400,400"
    AbsoluteLayout.LayoutFlags="None"
    HeightRequest="400"
    WidthRequest="400"
    InputTransparent="False"
    IsVisible="False"
    ZIndex="99">

            <ImageButton
                x:Name="NeonArrowUp"
                AbsoluteLayout.LayoutBounds="0.5,0.0,75,65"
                AbsoluteLayout.LayoutFlags="PositionProportional"
                BackgroundColor="Transparent"
                Clicked="OnMoveUpClicked"
                CommandParameter="Up"
                HeightRequest="100"
                Source="placementbuttup.svg"
                Style="{StaticResource NeonArrowStyle}"
                WidthRequest="100" />



            <!--  Down Button  -->
            <ImageButton
                x:Name="NeonArrowDown"
                AbsoluteLayout.LayoutBounds="0.5,0.9,44,44"
                AbsoluteLayout.LayoutFlags="PositionProportional"
                BackgroundColor="Transparent"
                Clicked="OnMoveDownClicked"
                CommandParameter="Down"
                HeightRequest="100"
                Source="placementbuttdown.svg"
                Style="{StaticResource NeonArrowStyle}"
                WidthRequest="100" />

            <!--  Left Button  -->
            <ImageButton
                x:Name="NeonArrowLeft"
                AbsoluteLayout.LayoutBounds="0.1,0.5,44,40"
                AbsoluteLayout.LayoutFlags="PositionProportional"
                BackgroundColor="Transparent"
                Clicked="OnMoveLeftClicked"
                CommandParameter="Left"
                HeightRequest="100"
                Source="placementbuttleft.svg"
                Style="{StaticResource NeonArrowStyle}"
                WidthRequest="100" />
            <!--  Right Button  -->
            <ImageButton
                x:Name="NeonArrowRight"
                AbsoluteLayout.LayoutBounds="0.9,0.5,44,44"
                AbsoluteLayout.LayoutFlags="PositionProportional"
                BackgroundColor="Transparent"
                Clicked="OnMoveRightClicked"
                CommandParameter="Right"
                HeightRequest="100"
                Source="placementbuttright.svg"
                Style="{StaticResource NeonArrowStyle}"
                WidthRequest="100" />

        </AbsoluteLayout>


        <!--  Feature Card (Main Device Info) - Centered in 400x400 container  -->
        <Border
            AbsoluteLayout.LayoutBounds="100,110,200,180"
            AbsoluteLayout.LayoutFlags="None"
            Background="{StaticResource PlacedDeviceAddButton}"
            HeightRequest="180"
            InputTransparent="False"
            ZIndex="3">
            <Border.StrokeShape>
                <RoundRectangle CornerRadius="16" />
            </Border.StrokeShape>

            <Grid InputTransparent="False" RowDefinitions="Auto,*,Auto">

                <!--  Device Info Container (Top)  -->
                <StackLayout
                    Grid.Row="0"
                    Margin="0,9,0,0"
                    HorizontalOptions="Center"
                    Spacing="4"
                    VerticalOptions="Start">

                    <!--  Device Name  -->
                    <Label
                        FontFamily="SpaceMonoBold"
                        FontSize="12"
                        HorizontalOptions="Center"
                        LineBreakMode="TailTruncation"
                        MaxLines="1"
                        Text="{Binding Name}"
                        TextColor="{StaticResource PlacedDeviceText}"
                        TextDecorations="Underline" />

                    <!--  IP Address  -->
                    <Label
                        FontFamily="SpaceMonoRegular"
                        FontSize="10"
                        HorizontalOptions="Center"
                        LineBreakMode="TailTruncation"
                        MaxLines="1"
                        Text="{Binding IpAddress}"
                        TextColor="{StaticResource PlacedDeviceTextSecondary}"
                        TextDecorations="Underline" />
                </StackLayout>

                <!--  Spacer for center area  -->
                <BoxView Grid.Row="1" BackgroundColor="Transparent" />

                <!--  Button Row at Bottom  -->
                <Grid
                    Grid.Row="2"
                    Margin="0,0,0,9"
                    ColumnDefinitions="Auto,Auto,Auto"
                    HorizontalOptions="Center"
                    RowDefinitions="Auto,Auto,Auto"
                    VerticalOptions="End">

                    <!--  Delete Device Button (Left)  -->
                    <ImageButton
                        Grid.Row="2"
                        Grid.RowSpan="1"
                        Grid.Column="0"
                        Aspect="Center"
                        BackgroundColor="{StaticResource PlacedDeviceDeleteButton}"
                        Clicked="OnDeleteDeviceClicked"
                        CornerRadius="22"
                        HorizontalOptions="Center"
                        MinimumHeightRequest="35"
                        MinimumWidthRequest="35"
                        Source="bin.svg"
                        VerticalOptions="Center" />

                    <!--  Large Open/Close Button (Center)  -->
                    <Border
                        Grid.Row="1"
                        Grid.RowSpan="2"
                        Grid.Column="1"
                        BackgroundColor="Transparent"
                        Stroke="Gray"
                        StrokeThickness="6">



                        <Border.StrokeShape>
                            <RoundRectangle CornerRadius="50" />
                        </Border.StrokeShape>

                        <ImageButton
                            Padding="10"
                            BackgroundColor="Green"
                            BorderColor="DarkGray"
                            BorderWidth="5"
                            Clicked="OnToggleDoorClicked"
                            CornerRadius="50"
                            HeightRequest="90"
                            HorizontalOptions="Center"
                            VerticalOptions="Center"
                            WidthRequest="90">
                            <ImageButton.Source>
                                <FontImageSource
                                    FontFamily="SpaceMonoBold"
                                    Glyph="{Binding IsDoorOpen, Converter={StaticResource BoolToOpenCloseConverter}}"
                                    Size="14"
                                    Color="Black" />
                            </ImageButton.Source>
                        </ImageButton>
                    </Border>


                    <!--  Move Device Button (Right)  -->
                    <ImageButton
                        x:Name="DevicePlacementModeBtn"
                        Grid.Row="2"
                        Grid.RowSpan="1"
                        Grid.Column="2"
                        Aspect="Center"
                        BackgroundColor="{StaticResource PlacedDeviceMoveButton}"
                        Clicked="OnMoveDeviceClicked"
                        CornerRadius="22"
                        HorizontalOptions="Center"
                        InputTransparent="False"
                        IsEnabled="True"
                        MinimumHeightRequest="35"
                        MinimumWidthRequest="35"
                        Source="move.svg"
                        VerticalOptions="Center">
                        <!--  Selection indicator as border around the move button  -->
                        <ImageButton.Triggers>
                            <DataTrigger
                                Binding="{Binding IsSelected}"
                                TargetType="ImageButton"
                                Value="True">
                                <Setter Property="BorderColor" Value="Yellow" />
                                <Setter Property="BorderWidth" Value="3" />
                            </DataTrigger>
                            <DataTrigger
                                Binding="{Binding IsSelected}"
                                TargetType="ImageButton"
                                Value="False">
                                <Setter Property="BorderColor" Value="Transparent" />
                                <Setter Property="BorderWidth" Value="0" />
                            </DataTrigger>
                        </ImageButton.Triggers>
                    </ImageButton>
                </Grid>
            </Grid>
        </Border>
    </AbsoluteLayout>
</ContentView>
